name: Test-Project
repo_url: https://github.com/some-user/my-test-project
version: 0.1.0
build_stage:
  strategy:
    strict: true
  steps:
    - name: Step 1
      category: build
      command: some-command
      shell: /bin/bash
      working_dir: .
      args: []
    - name: Step 2
      category: test
      command: some-command
      shell: /bin/bash
      working_dir: .
      args: []
    - name: Step 3
      category: deploy
      command: some-command
      shell: /bin/bash
      working_dir: .
      args: []
